# plant-disease-detection-system
An end-to-end deep learning system for plant leaf disease detection using transfer learning. The project includes optimized CNN training with TensorFlow, fine-tuning on a multi-class plant disease dataset, and a FastAPI backend for real-time image-based disease prediction.
